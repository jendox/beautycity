{$DOMAIN} {
    encode gzip zstd

    handle_path /static/* {
        root * /staticfiles
        file_server
    }

    handle_path /media/* {
        root * /media
        file_server
    }

    handle {
        reverse_proxy web:8000
    }
}

